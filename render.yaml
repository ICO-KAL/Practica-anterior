services:
  - type: web
    name: practica-anterior-app
    runtime: docker
    plan: free
    autoDeploy: false
    healthCheckPath: /health
    envVars:
      - key: FALLBACK_MESSAGE
        value: Aplicación desplegada correctamente en Render.
